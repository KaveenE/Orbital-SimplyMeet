import 'package:flutter/material.dart';

class GroupWidget extends StatelessWidget {
  const GroupWidget();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Guess what's orange and rhymes with parrot? Carrot!"),
    );
  }
}
